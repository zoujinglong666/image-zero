server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: turing-drawing
  config:
    import: optional:dotenv[.env]

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:turing_drawing}?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    default-property-inclusion: non_null

  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB

# MyBatis Plus 配置
mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    call-setters-on-nulls: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:dev-only-secret-key-please-change-in-production-min-32-chars}
  expiration: 7200000
  refresh-expiration: 604800000

# Tencent Cloud COS
cos:
  secret-id: ${COS_SECRET_ID:}
  secret-key: ${COS_SECRET_KEY:}
  region: ${COS_REGION:ap-guangzhou}
  bucket: ${COS_BUCKET:}
  domain: ${COS_DOMAIN:}
  upload-dir: ${COS_UPLOAD_DIR:./uploads}

# OpenAI Configuration (OpenRouter 兼容)
openai:
  api-key: ${OPENAI_API_KEY:}
  base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}

# AI 模型配置
ai:
  analyze-model: ${AI_ANALYZE_MODEL:nvidia/nemotron-nano-12b-v2-vl:free}
  generate-model: ${AI_GENERATE_MODEL:nvidia/nemotron-nano-12b-v2-vl:free}

# Google Gemini Configuration
gemini:
  api-key: ${GEMINI_API_KEY:}

# WeChat Configuration
wechat:
  app-id: ${WECHAT_APP_ID:}
  app-secret: ${WECHAT_APP_SECRET:}
  mini-program-app-id: ${WECHAT_MINI_PROGRAM_APP_ID:}
  mini-program-app-secret: ${WECHAT_MINI_PROGRAM_APP_SECRET:}

# CORS
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:}

# Rate Limiter
rate-limiter:
  ai-per-minute: ${RATE_LIMIT_AI:10}
  auth-per-minute: ${RATE_LIMIT_AUTH:20}
  community-per-minute: ${RATE_LIMIT_COMMUNITY:60}
  upload-per-minute: ${RATE_LIMIT_UPLOAD:15}
  window-seconds: 60

# SpringDoc OpenAPI
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
  health:
    diskspace:
      enabled: true

# Logging
logging:
  level:
    root: INFO
    com.turing.drawing: DEBUG
    com.turing.drawing.mapper: DEBUG